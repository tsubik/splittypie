{{#if anyEvents}}
  <h4 class="text-center">Your Previous Events</h4>

  <div class="list-group">
    {{#each events as |event|}}
      {{#link-to "event.index" event.id class="list-group-item btn btn-default"}}
        <strong>{{event.name}}</strong>
      {{/link-to}}
    {{/each}}
  </div>
{{/if}}
