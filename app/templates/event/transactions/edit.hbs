{{#if (is-equal form.formType "transfer")}}
  {{transfer-form transfer=form delete="delete" title="View Transfer"}}
{{else}}
  {{transaction-form transaction=form users=users delete="delete" modelUpdated="modelUpdated" title="Edit Transaction"}}
{{/if}}
