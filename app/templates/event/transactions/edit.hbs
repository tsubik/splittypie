{{#if (is-equal form.formType "transfer")}}
  {{transfer-form transfer=form onDelete=(route-action "delete") title="View Transfer"}}
{{else}}
  {{transaction-form transaction=form users=users onDelete=(route-action "delete") onModelUpdated=(route-action "modelUpdated") title="Edit Transaction"}}
{{/if}}
