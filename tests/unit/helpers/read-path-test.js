import { readPath } from '../../../helpers/read-path';
import { module, test } from 'qunit';

module('Unit | Helper | read path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = readPath(42);
  assert.ok(result);
});
