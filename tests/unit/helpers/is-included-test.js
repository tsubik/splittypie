import { isIncluded } from "../../../helpers/is-included";
import { module, test } from "qunit";

module("Unit | Helper | is included");

// Replace this with your real tests.
test("it works", function(assert) {
    let result = isIncluded(42);
    assert.ok(result);
});
